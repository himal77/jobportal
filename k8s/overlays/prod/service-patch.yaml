apiVersion: v1
kind: Service
metadata:
  name: jobportal
spec:
  selector:
    app: jobportal
    version: green
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
  type: NodePort