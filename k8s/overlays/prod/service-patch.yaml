apiVersion: v1
kind: Service
metadata:
  name: jobportal
spec:
  selector:
    app: jobportal
    version: blue        # ← GitHub Actions changes this
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
  type: NodePort