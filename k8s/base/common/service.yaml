apiVersion: v1
kind: Service
metadata:
  name: jobportal
spec:
  selector:
    app: jobportal       # ← no version label, just routes to any jobportal pod
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP        # ← internal only, port exposed via overlay patch