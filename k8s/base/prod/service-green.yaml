apiVersion: v1
kind: Service
metadata:
  name: jobportal-green
  namespace: default
spec:
  selector:
    app: jobportal
    version: green
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30084   # ← localhost:30082 (if kind config has this port)
  type: NodePort