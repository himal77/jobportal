apiVersion: apps/v1
kind: Deployment
metadata:
  name: jobportal
  namespace: default
spec:
  replicas: 2
  selector:
    matchLabels:
      app: jobportal
  template:
    metadata:
      labels:
        app: jobportal
    spec:
      containers:
        - name: jobportal
          image: himal77/jobportal:1.1.0
          ports:
            - containerPort: 8080
          resources:
            requests:
              memory: '256Mi'
              cpu: '250m'
            limits:
              memory: '512Mi'
              cpu: '500m'
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 30
